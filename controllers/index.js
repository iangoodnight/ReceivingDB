/**
 * Collected controllers for convenience
 */

'use strict';

module.exports = {
  auth: require('./auth.controller'),
  entry: require('./entry.controller'),
  user: require('./user.controller'),
};
